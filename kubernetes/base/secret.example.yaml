# Template only. Do NOT commit real secrets.
# Create the runtime Secret before deployment: see scripts/create-k8s-secret.sh
apiVersion: v1
kind: Secret
metadata:
  name: taskflow-secret
  namespace: taskflow
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME"
  DATABASE_URL: "postgresql://taskflow:REPLACE_ME@postgres:5432/taskflow_db"
  SECRET_KEY: "REPLACE_ME_WITH_A_RANDOM_32_PLUS_CHARACTER_SECRET"
